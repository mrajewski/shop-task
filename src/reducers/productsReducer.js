const initialState = {
    electric: [],
    bass: [],
    acoustic: [],
    ukulele: [],
    others: []
}

export default (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}